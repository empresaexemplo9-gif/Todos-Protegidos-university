# Banco de dados (Vercel Postgres / Neon)
# Na Vercel, a integração Postgres/Neon injeta esta variável automaticamente.
DATABASE_URL=postgres://usuario:senha@host/banco?sslmode=require

# Senha de acesso do painel administrativo (login da equipe)
ADMIN_PASSWORD=troque-esta-senha
